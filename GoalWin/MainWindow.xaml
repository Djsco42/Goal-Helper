<Window x:Name="Win" x:Class="GoalWin.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GoalWin"
        mc:Ignorable="d"
        Title="MainWindow" Height="500" Width="900" 
        Background="#FFDE7575"
        MinWidth="900"
        MinHeight="500"
        
        >

    <Grid x:Name="Ggrid" Margin="36,36,638,36" MinHeight="270" MinWidth="200" HorizontalAlignment="Left" VerticalAlignment="Top" >

        <DatePicker x:Name="GDate"  Height="30" Width="115" MinHeight="30" MinWidth="115" BorderBrush="{x:Null}" Foreground="Black" Margin="50,112,50,255"/>
        <Button x:Name="Gsum" Content="Summit Goal" Margin="40,230,40,10"  BorderBrush="{x:Null}" Background="#FF1DCA5F" FontWeight="Bold" Click="Gsum_Click" Height="30" Width="120" MinHeight="30" MinWidth="120"/>
        <Label x:Name="Glable" Content="Setting Up A Goal" Margin="16,10,10,350" Background="{x:Null}" Foreground="#FFF34444" FontWeight="Bold" FontSize="20" Height="45" Width="200"/>
        <Rectangle HorizontalAlignment="Stretch"  VerticalAlignment="Stretch"  Fill="#FF1BBCAD" Panel.ZIndex="-1" RadiusX="30" RadiusY="30" Margin="0,0,0,0"/>
        <TextBox x:Name="Gname" Margin="22,64,16,305" BorderBrush="{x:Null}">
            <TextBox.Style>
                <Style TargetType="TextBox">
                    <Style.Resources>
                        <VisualBrush x:Key="WatermarkBrush" Opacity="0.5" Stretch="None">
                            <VisualBrush.Visual>
                                <TextBlock Text="Add A Title" FontStyle="Italic" Foreground="Black" FontSize="18" />
                            </VisualBrush.Visual>
                        </VisualBrush>
                    </Style.Resources>
                    <Style.Triggers>
                        <Trigger Property="Text" Value="">
                            <Setter Property="Background" Value="{StaticResource WatermarkBrush}" />
                        </Trigger>
                        <Trigger Property="IsKeyboardFocused" Value="True">
                            <Setter Property="Background" Value="White" />
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </TextBox.Style>
        </TextBox>


    </Grid>
</Window>
